import { TileStateConverterPipe } from './tile-state-converter.pipe';

describe('TileStateConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new TileStateConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
